{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build",
    "watchPatterns": ["server/**", "client/**", "package.json"]
  },
  "deploy": {
    "startCommand": "npm start",
    "healthcheckPath": "/api/auth/me",
    "restartPolicyType": "ON_FAILURE"
  }
}
